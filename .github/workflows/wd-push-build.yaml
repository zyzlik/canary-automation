name: Build and Deploy

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: [ubuntu-latest]

    steps:
      - uses: actions/checkout@v3

      - name: docker build
        run: |
          docker build . -t zyzlik/canary-automation:latest
      
      - name: login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: push
        run: |
          docker push zyzlik/canary-automation:latest
  deploy:
    runs-on: [ubuntu-latest]
    needs: [build]
    steps:
      - name: configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_KEY }}
          aws-region: us-east-2
          role-to-assume: arn:aws:iam::801176112578:role/eksClusterRole
      - name: configure kube config
        run: |
          aws eks update-kubeconfig --name canary-automation
          kubectl config set-context arn:aws:eks:us-east-2:801176112578:cluster/canary-automation
          cat ~/.kube/config
          kubectl describe -n kube-system configmap/aws-auth

          # kubectl get ns
          # helm3 upgrade --install --create-namespace canary-automation ./kubernetes